# Copy this file to `.dev.vars` for local development (`npm run dev`).
# `.dev.vars` is git-ignored — never commit real values.

SECRET=replace-me-with-at-least-16-chars

# Optional overrides for local testing
# ALLOWED_TARGET_HOSTS=api.github.com
# RATE_LIMIT_RPM=100
